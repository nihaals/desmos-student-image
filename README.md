# Desmos Student Image

## Config

### `class_id`

Search for `classId` or check save endpoint payload

### `student_id`

End of URL, search for `studentId` or check save endpoint payload

### `sketch_field_id`

Get the value of `data-dcg-component-id` or check save endpoint payload
